using UnityEngine;

public class Epos : MonoBehaviour
{

    public GameObject GoTo;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
